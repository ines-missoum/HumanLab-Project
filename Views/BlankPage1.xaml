<Page
    x:Class="humanlab.Views.BlankPage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:humanlab.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Canvas>
            <ScrollViewer x:Name="scrollview" ZoomMode="Enabled" HorizontalScrollBarVisibility="Auto"  SizeChanged="scrollview_SizeChanged" Background="Red">
                <Image Source="../Assets/StoreLogo.png"  x:Name="image" ManipulationMode = "TranslateX, TranslateY, Scale" ManipulationDelta = "t_ManipulationDelta" >
                </Image>
                <ScrollViewer.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform x:Name="scaleTransform" />
                        <RotateTransform x:Name="rotateTransform" />
                        <TranslateTransform x:Name="translateTransform" />
                    </TransformGroup>
                </ScrollViewer.RenderTransform>
            </ScrollViewer>
        </Canvas>
    </Grid>
</Page>
